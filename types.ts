export interface SongMetadata {
  path: string;
  artist: string;
  title: string;
  genres: string[];
  year: number;
  artwork: string;
  album: string;
  bitrate: number;
  duration: number;
  format: string;
}
